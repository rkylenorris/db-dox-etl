# DB DOX ETL

---

## Summary

This is the ETL process supporting a database documentation application named `db-dox`. It reads metadata from the source databases, transforms it to fit `sqlalchemy` models before inserting it into the documentation database.
